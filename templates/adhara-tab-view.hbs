<div class="{{containerClass}}">
    <div class="{{tabNavClass}}">
        <ul class="{{tabListClass}}">
            {{#each tabsList}}
                <li class="{{className}}">
                    <a href="{{link}}">
                        <!--<div class="icon-circle">
                            <i class="tab-icon ti-x {{icon}}"></i>
                        </div>-->
                        {{name}}
                    </a>
                </li>
            {{/each}}
        </ul>
        <div class="tab-content">
            <div class="tab-pane active in">
            </div>
        </div>
    </div>
</div>